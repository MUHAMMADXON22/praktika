
const userColor = prompt("Введите цвет для фона :");
if (userColor) ;
const textColor = prompt("Введите цвет для текста:");
if (textColor) ;
document.body.style.backgroundColor = userColor;
document.body.style.color = textColor;